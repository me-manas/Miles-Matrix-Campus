//-----corresponds to API status
enum ApiStatus { init, loading, empty, success, error }

//-----corresponds to API request types
enum ApiRequest { get, put, post, delete }

//------corresponds to Login type
enum LoginType { mobile, email }

//-------corresponds to university squad in onboarding
enum UniversityEnrollmentType { enrolled, yetToEnroll }
